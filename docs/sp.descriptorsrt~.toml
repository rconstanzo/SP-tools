[object]
name = "sp.descriptorsrt~"
module = "SP-Tools"
category = ""
digest = "Analyzes audio for several audio desriptors based on audio input"
seealso =  ['sp.descriptorframe', 'sp.descriptors~', 'sp.melbandframe', 'sp.melbands~', 'sp.melbandsrt~', 'sp.mfcc~', 'sp.mfccframe', 'sp.mfccrt~', 'sp.onsetframe~', 'sp.realtimeframe~']
description = "Analyzes audio for several audio desriptors based on audio input."
discussion = """
    <h4><openfilelink filename="SP-Tools Overview.maxpat">Open the Overview Patch</openfilelink></h4>
    <p>The <o>sp.descriptorsrt~</o> object outputs loudness, centroid, spectral flatness, and pitch along with the derivatives for loudness/centroid/flatness and confidence for pitch.</p>
"""

[metadata]
author = "Rodrigo Constanzo, Flucoma Project"
tags = ['Machine Learning', 'Drums']

[inlets]
[inlets.0]
type="signal"
digest="Audio and control in"
description="Audio input to analyze."

[outlets]
[outlets.0]
type="list"
digest="Descriptors as list"
description="Analyzed descriptors in list form."

[outlets.1]
type="list"
digest="Descriptors as <o>buffer~</o>"
description="Analyzed descriptors in <o>buffer~</o> form."

[messages.load]
digest = "Load a pre-trained concat <b>.json</b> file"
description = "Opens file dialog to select pre-trained concat <b>.json</b> file."

[messages.read]
digest = "Read a pre-trained concat <b>.json</b> file"
description = "Reads a pre-trained concat <b>.json</b> file directly from a relative or absolute path."